# Copy this file to .env.android and fill in your values.
# .env.android is gitignored — never commit the real file.
#
# Generate KEYSTORE_BASE64 with:
#   base64 < your-release.jks

KEYSTORE_BASE64=
KEY_ALIAS=your-key-alias
KEY_PASSWORD=your-key-password
STORE_PASSWORD=your-store-password
